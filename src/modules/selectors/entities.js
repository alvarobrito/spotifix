// selectors
export const tracksEntity = state =>
  state.entities.tracks;

export const albumsEntity = state =>
  state.entities.albums;

export const artistsEntity = state =>
  state.entities.artists;
